import { DateTimeFormatPipe } from './date-time-format-pipe';

describe('DateTimeFormatPipe', () => {
  it('should create an instance', () => {
    expect(new DateTimeFormatPipe()).toBeTruthy();
  });
});
